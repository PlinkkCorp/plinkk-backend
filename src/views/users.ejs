<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Plinkk · Utilisateurs</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-950 text-slate-100 min-h-dvh">
    <header class="border-b border-slate-800 bg-slate-900/60 backdrop-blur sticky top-0 z-40">
      <div class="max-w-7xl mx-auto px-4 py-3 flex items-center gap-4">
        <a href="/" class="flex items-center gap-2">
          <img src="/public/images/logo.png" alt="Plinkk" class="h-8 w-8 rounded" />
          <span class="font-semibold">Plinkk</span>
        </a>
      </div>
    </header>

    <main class="max-w-7xl mx-auto p-4 space-y-4">
      <div class="flex items-center justify-between">
        <h1 class="text-lg font-semibold">Tous les profils hébergés</h1>
        <p class="text-xs text-slate-400">Total: <%= users.length %></p>
      </div>
      <% if (users.length === 0) { %>
        <div class="rounded border border-dashed border-slate-700 bg-slate-900/40 p-6 text-sm text-slate-300 text-center">
          Aucun utilisateur pour le moment.
        </div>
      <% } else { %>
        <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
          <% users.forEach(u => { %>
            <li class="rounded-lg border border-slate-800 bg-slate-900/60 p-4">
              <div class="flex items-center gap-3">
                <div class="h-9 w-9 rounded bg-slate-800 border border-slate-700 flex items-center justify-center text-xs">@</div>
                <div class="min-w-0">
                  <div class="font-medium truncate"><%= u.userName %> <span class="text-slate-400">(@<%= u.id %>)</span></div>
                  <div class="text-xs text-slate-400 truncate"><%= u.email %></div>
                </div>
              </div>
              <div class="mt-3 flex items-center gap-2">
                <a class="px-3 py-2 text-xs rounded-md bg-slate-800 hover:bg-slate-700 border border-slate-700" href="/<%= u.id %>">Voir le profil</a>
              </div>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </main>
  </body>
  </html>
