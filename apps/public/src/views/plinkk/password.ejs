<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accès protégé - <%= page.title || slug %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="icon" href="https://cdn.plinkk.fr/logo.svg" />
    <style>
        body {
            background-color: #f3f4f6;
            color: #1f2937;
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        .password-card {
            background: white;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        .logo {
            width: 64px;
            height: 64px;
            border-radius: 50%;
            margin-bottom: 1.5rem;
            object-fit: cover;
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            margin-top: 0;
        }

        p {
            color: #6b7280;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        input[type="password"] {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            font-size: 1rem;
            outline: none;
            transition: border-color 0.2s;
            box-sizing: border-box;
        }

        input[type="password"]:focus {
            border-color: #2563eb;
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
        }

        button {
            width: 100%;
            padding: 0.75rem;
            background-color: #2563eb;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        button:hover {
            background-color: #1d4ed8;
        }

        .error {
            background-color: #fee2e2;
            color: #b91c1c;
            padding: 0.75rem;
            border-radius: 8px;
            margin-bottom: 1rem;
            font-size: 0.875rem;
        }
    </style>
</head>

<body>
    <div class="password-card">
        <% if (page.avatar) { %>
            <img src="<%= page.avatar %>" alt="Avatar" class="logo">
            <% } else { %>
                <img src="https://cdn.plinkk.fr/logo.svg" alt="Plinkk" class="logo">
                <% } %>

                    <h1>Accès Protégé</h1>
                    <p>Cette page est protégée par un mot de passe.</p>

                    <h1>Accès Protégé</h1>
                    <p>Cette page est protégée par un mot de passe.</p>

                    <% if (error) { %>
                        <div class="error">
                            <%= error %>
                        </div>
                        <% } %>

                            <form action="/plinkk/verify-password" method="POST">
                                <input type="hidden" name="username" value="<%= username %>">
                                <input type="hidden" name="identifier"
                                    value="<%= typeof identifier !== 'undefined' ? identifier : '' %>">

                                <div class="form-group">
                                    <input type="password" name="password" placeholder="Mot de passe" required
                                        autofocus>
                                </div>
                                <button type="submit">Accéder</button>
                            </form>
    </div>
</body>

</html>