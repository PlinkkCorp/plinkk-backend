    <!-- Header -->
    <header class="sticky top-0 z-30 bg-[#0B1020]/60 backdrop-blur border-b border-slate-800/50">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
        <a href="/" class="flex items-center gap-2">
          <img src="/public/images/logo.svg" alt="Plinkk" class="h-8 w-8 rounded" />
          <span class="font-semibold brand-gradient">Plinkk</span>
        </a>
        <nav class="hidden md:flex items-center gap-6 text-sm text-slate-300">
          <a href="/about" class="hover:text-white">À propos</a>
          
          <a href="/users" class="hover:text-white">Utilisateurs</a>
          <a href="/dashboard" class="hover:text-white">Dashboard</a>
        </nav>
        <div class="hidden md:flex items-center gap-3">
          <% if (typeof currentUser !== 'undefined' && currentUser) { %>
            <div class="flex items-center gap-2 relative" id="accountMenuRoot">
              <a href="/dashboard" class="px-3 py-2 text-sm rounded-md bg-slate-800/60 hover:bg-slate-800 border border-slate-700">Mon dashboard</a>
              <button id="accountBtn" class="flex items-center gap-2 px-2.5 py-1.5 rounded-full border border-slate-700/70 bg-slate-800/60 hover:bg-slate-800 focus:outline-none">
                <span class="inline-flex h-7 w-7 items-center justify-center rounded-full bg-indigo-700/70 text-xs font-semibold overflow-hidden" data-avatar>
                  <%- include('avatar.ejs', { user: currentUser }) %>
                </span>
                <span class="hidden sm:inline text-sm text-slate-200 max-w-[160px] truncate">@<%= currentUser.userName || currentUser.id %></span>
                <svg class="w-4 h-4 text-slate-400" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.24 4.5a.75.75 0 01-1.08 0l-4.24-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd"/></svg>
              </button>
              <div id="accountPanel" class="absolute right-0 top-full mt-2 w-80 rounded-xl border border-slate-800/70 bg-slate-900/95 backdrop-blur shadow-xl p-2 hidden">
                <!-- Account card -->
                <div class="flex items-center gap-3 px-3 py-2">
                  <div class="h-10 w-10 rounded-full grid place-items-center bg-indigo-700/70 font-semibold text-white overflow-hidden" data-avatar>
                    <%- include('avatar.ejs', { user: currentUser }) %>
                  </div>
                  <div class="min-w-0">
                    <div class="flex items-center gap-2 min-w-0">
                      <p class="font-medium truncate text-slate-100"><%= currentUser.userName || currentUser.id %></p>
                      <span class="text-[10px] px-1.5 py-0.5 rounded bg-slate-800/80 border border-slate-700 text-slate-300 truncate">@<%= currentUser.id %></span>
                    </div>
                    <% if (currentUser.email) { %>
                      <p class="text-[11px] text-slate-400 truncate"><%= currentUser.email %></p>
                    <% } %>
                  </div>
                </div>
                <div class="px-3 pb-2">
                  <a href="/dashboard/account" class="inline-flex items-center gap-2 px-3 py-1.5 text-xs rounded-md bg-slate-800/70 hover:bg-slate-800 border border-slate-700 text-slate-200">Gérer mon compte</a>
                </div>
                <div class="my-1 border-t border-slate-800"></div>
                <!-- Quick links -->
                <div class="grid grid-cols-2 gap-2 px-2 py-2">
                  <a href="/<%= currentUser.id %>" class="px-3 py-2 text-xs rounded-md border border-slate-700 bg-slate-800/60 hover:bg-slate-800">Voir mon profil</a>
                  <a href="/dashboard/cosmetics" class="px-3 py-2 text-xs rounded-md border border-slate-700 bg-slate-800/60 hover:bg-slate-800">Cosmétiques</a>
                </div>
                <div class="my-1 border-t border-slate-800"></div>
                <div class="px-2 py-1">
                  <a href="/logout" class="block w-full text-left px-3 py-2 text-sm rounded-md hover:bg-slate-800/80">Se déconnecter</a>
                </div>
              </div>
            </div>
          <% } else { %>
            <a href="/login" class="px-3 py-2 text-sm text-slate-200 hover:text-white btn-ghost rounded-md">Se connecter</a>
            <a href="/login#signup" class="px-3 py-2 text-sm rounded-md btn-brand shadow">S'inscrire</a>
          <% } %>
        </div>
        <!-- Mobile -->
        <button id="menuBtn" class="md:hidden inline-flex items-center justify-center h-9 w-9 rounded-md border border-slate-800/60 text-slate-300 hover:text-white">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="h-5 w-5"><path d="M3.75 6.75h16.5v1.5H3.75v-1.5Zm0 4.5h16.5v1.5H3.75v-1.5Zm0 4.5h16.5v1.5H3.75v-1.5Z"/></svg>
        </button>
      </div>
      <div id="mobileMenu" class="md:hidden hidden border-t border-slate-800/60">
        <div class="px-4 py-3 space-y-2 text-sm">
          <a href="/about" class="block py-1 text-slate-300 hover:text-white">À propos</a>
          
          <a href="/users" class="block py-1 text-slate-300 hover:text-white">Utilisateurs</a>
          <a href="/dashboard" class="block py-1 text-slate-300 hover:text-white">Dashboard</a>
          <% if (typeof currentUser !== 'undefined' && currentUser) { %>
            <div class="pt-2 space-y-2">
              <div class="flex items-center gap-2">
                <div class="h-8 w-8 rounded-full grid place-items-center bg-indigo-700/70 font-semibold text-white overflow-hidden" data-avatar>
                  <%- include('./avatar', { user: currentUser }) %>
                </div>
                <div class="min-w-0">
                  <p class="text-sm font-medium text-slate-200 truncate"><%= currentUser.userName || currentUser.id %></p>
                  <% if (currentUser.email) { %>
                    <p class="text-[11px] text-slate-400 truncate"><%= currentUser.email %></p>
                  <% } %>
                </div>
              </div>
              <div class="grid grid-cols-2 gap-2">
                <a class="text-center px-3 py-2 rounded bg-slate-800/60 hover:bg-slate-800 border border-slate-700" href="/dashboard">Dashboard</a>
                <a class="text-center px-3 py-2 rounded bg-slate-800/60 hover:bg-slate-800 border border-slate-700" href="/dashboard/account">Compte</a>
                <a class="text-center px-3 py-2 rounded bg-slate-800/60 hover:bg-slate-800 border border-slate-700" href="/dashboard/cosmetics">Cosmétiques</a>
                <a class="text-center px-3 py-2 rounded bg-slate-800/60 hover:bg-slate-800 border border-slate-700" href="/<%= currentUser.id %>">Mon profil</a>
              </div>
              <div class="flex items-center justify-between px-1">
                <span>Public</span>
                <label class="inline-flex items-center cursor-pointer">
                  <input id="visibilityToggleMobile" type="checkbox" class="sr-only" <%= currentUser.isPublic ? 'checked' : '' %> />
                  <span class="w-10 h-6 bg-slate-700 rounded-full p-1 transition-all">
                    <span class="block w-4 h-4 bg-white rounded-full transition-transform <%= currentUser.isPublic ? 'translate-x-4' : '' %>"></span>
                  </span>
                </label>
              </div>
              <a class="block text-center px-3 py-2 rounded hover:bg-slate-800/80" href="/<%= currentUser.id %>">Voir mon profil</a>
              <a class="block text-center px-3 py-2 rounded hover:bg-slate-800/80" href="/logout">Se déconnecter</a>
            </div>
          <% } else { %>
            <div class="pt-2 flex gap-2">
              <a href="/login" class="flex-1 text-center px-3 py-2 text-slate-200 hover:text-white border border-slate-800/60 rounded btn-ghost">Connexion</a>
              <a href="/login#signup" class="flex-1 text-center px-3 py-2 rounded text-white btn-brand">Inscription</a>
            </div>
          <% } %>
        </div>
      </div>
    </header>
    <script>
      (function(){
        const postVisibility = (on) => fetch('/api/me/visibility', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ isPublic: !!on })});
        const t = document.getElementById('visibilityToggle');
        const tm = document.getElementById('visibilityToggleMobile');
        if (t) t.addEventListener('change', () => postVisibility(t.checked).catch(()=>{ t.checked=!t.checked; }));
        if (tm) tm.addEventListener('change', () => postVisibility(tm.checked).catch(()=>{ tm.checked=!tm.checked; }));
        const menuBtn = document.getElementById('menuBtn');
        const mobileMenu = document.getElementById('mobileMenu');
        if (menuBtn && mobileMenu) menuBtn.addEventListener('click', ()=> mobileMenu.classList.toggle('hidden'));

        // Account dropdown open/close
        const accountBtn = document.getElementById('accountBtn');
        const accountPanel = document.getElementById('accountPanel');
        const root = document.getElementById('accountMenuRoot');
        const closeMenu = () => { accountPanel?.classList.add('hidden'); accountBtn?.setAttribute('aria-expanded','false'); };
        const openMenu = () => { accountPanel?.classList.remove('hidden'); accountBtn?.setAttribute('aria-expanded','true'); };
        if (accountBtn && accountPanel && root) {
          accountBtn.addEventListener('click', (e)=>{ e.stopPropagation(); const isOpen = !accountPanel.classList.contains('hidden'); if (isOpen) closeMenu(); else openMenu(); });
          document.addEventListener('click', (e)=>{ if (!root.contains(e.target)) closeMenu(); });
          document.addEventListener('keydown', (e)=>{ if (e.key === 'Escape') closeMenu(); });
        }
      })();
    </script>
