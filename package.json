{
  "scripts": {
    "prisma:migratedev": "prisma migrate dev",
    "prisma:dev": "prisma dev",
    "prisma:studio": "prisma studio",
    "prisma:init": "npx prisma migrate deploy --schema=packages/prisma/prisma/schema.prisma",
    "postinstall": "pnpm --filter @plinkk/prisma prisma generate",
    "set-admin": "node scripts/set-admin.cjs",
    "migrate:plinkks": "node scripts/migrate-plinkks.cjs",
    "delete_inactive": "node scripts/delete_inactive_user.js",
    "seed-permissions": "node scripts/seed-permissions.js",
    "prisma:generate": "pnpm --filter @plinkk/prisma prisma generate",
    "build": "pnpm -r build",
    "launch-all": "concurrently \"pnpm --filter @plinkk/dashboard dev\" \"pnpm --filter @plinkk/public dev\""
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "pg": "^8.18.0",
    "prisma": "^7.3.0"
  },
  "dependencies": {
    "@fastify/secure-session": "^8.3.0",
    "@prisma/client": "^7.3.0",
    "dotenv": "^17.2.4"
  },
  "prisma": {
    "schema": "packages/prisma/prisma/schema.prisma"
  },
  "private": true
}
