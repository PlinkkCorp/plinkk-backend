<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= ((typeof title !=='undefined' && title) ? title + ' — ' : '' ) %>Plinkk
  </title>
  <% const desc=(typeof description !=='undefined' && description) ? description
    : 'Crée ta page de liens élégante et personnalisée avec Plinkk.' ; %>
    <% const robotsVal=(typeof robots !=='undefined' && robots) ? robots : 'index,follow' ; %>
      <% const img=(typeof image !=='undefined' && image) ? image : 'https://s3.marvideo.fr/plinkk-image/logo.svg' ; %>
        <% const canon=(typeof canonical !=='undefined' && canonical) ? canonical : null; %>
          <meta name="description" content="<%= desc %>" />
          <meta name="robots" content="<%= robotsVal %>" />
          <meta name="theme-color" content="#0B1020" />
          <link rel="icon" href="https://s3.marvideo.fr/plinkk-image/logo.svg" />
          <meta property="og:site_name" content="Plinkk" />
          <meta property="og:type" content="website" />
          <meta property="og:title"
            content="<%= ((typeof title !== 'undefined' && title) ? title + ' — ' : '') %>Plinkk" />
          <meta property="og:description" content="<%= desc %>" />
          <meta property="og:image" content="<%= img %>" />
          <% if (canon) { %>
            <meta property="og:url" content="<%= canon %>" />
            <link rel="canonical" href="<%= canon %>" />
            <% } %>
              <meta name="twitter:card" content="summary_large_image" />
              <meta name="twitter:title"
                content="<%= ((typeof title !== 'undefined' && title) ? title + ' — ' : '') %>Plinkk" />
              <meta name="twitter:description" content="<%= desc %>" />
              <meta name="twitter:image" content="<%= img %>" />
              <script defer src="/umami_script.js" data-website-id="69e21905-d1fc-48c8-a64e-26a6ad5ad089" data-host-url="https://analytics.plinkk.fr"></script>
              <script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
              <style>
                * {
                  font-family: Segoe UI, Tahoma, Geneva, Verdana, sans-serif;
                }

                :root {
                  --ease-out-quad: cubic-bezier(.22, 1, .36, 1);
                  --brand-1: #4338CA;
                  --brand-2: #A21CAF;
                  --brand-3: #E11D48;
                  --brand-sky-1: #0EA5E9;
                  --brand-sky-2: #22D3EE;
                  --brand-emerald: #34D399;
                  --surface-950: #0B1020;
                  --surface-900: #0b1324;
                  --stroke-800: #1f2937;
                }

                body::before {
                  content: "";
                  position: fixed;
                  inset: 0;
                  pointer-events: none;
                  z-index: -1;
                  background:
                    radial-gradient(1200px 700px at 10% 10%, rgba(67, 56, 202, .18), transparent 60%),
                    radial-gradient(1000px 600px at 80% 80%, rgba(162, 28, 175, .15), transparent 60%),
                    radial-gradient(900px 540px at 50% 10%, rgba(225, 29, 72, .10), transparent 60%);
                  filter: blur(0.5px);
                }

                .brand-gradient {
                  background: linear-gradient(90deg, var(--brand-1), var(--brand-2), var(--brand-3));
                  -webkit-background-clip: text;
                  background-clip: text;
                  -webkit-text-fill-color: transparent;
                }

                .brand-gradient-sky {
                  background: linear-gradient(90deg, var(--brand-sky-1), var(--brand-sky-2));
                  -webkit-background-clip: text;
                  background-clip: text;
                  -webkit-text-fill-color: transparent;
                }

                .btn-brand {
                  background: linear-gradient(90deg, var(--brand-1), var(--brand-2), var(--brand-3));
                  color: white
                }

                .btn-brand:hover {
                  filter: brightness(1.05);
                  box-shadow: 0 8px 24px rgba(162, 28, 175, .35)
                }

                .btn-ghost {
                  border: 1px solid rgba(148, 163, 184, .35);
                  color: #e5e7eb
                }

                .btn-ghost:hover {
                  color: white;
                  border-color: rgba(148, 163, 184, .6)
                }

                .chip {
                  background-color: #0f172a;
                  border: 1px solid rgba(31, 41, 55, 1);
                  color: #cbd5e1
                }

                .card-surface {
                  background-color: rgba(11, 19, 36, .9);
                  border: 1px solid rgba(31, 41, 55, 1)
                }

                .ring-brand {
                  box-shadow: 0 0 0 1px rgba(31, 41, 55, .5), inset 0 0 0 1px rgba(67, 56, 202, .35)
                }

                @keyframes fade-in {
                  from {
                    opacity: 0
                  }

                  to {
                    opacity: 1
                  }
                }

                @keyframes slide-up {
                  from {
                    opacity: 0;
                    transform: translateY(8px)
                  }

                  to {
                    opacity: 1;
                    transform: translateY(0)
                  }
                }

                @keyframes pop-in {
                  from {
                    opacity: 0;
                    transform: scale(.98)
                  }

                  to {
                    opacity: 1;
                    transform: scale(1)
                  }
                }

                .animate-fade-in {
                  animation: fade-in .36s var(--ease-out-quad) both
                }

                .animate-slide-up {
                  animation: slide-up .42s var(--ease-out-quad) both
                }

                .animate-pop-in {
                  animation: pop-in .28s var(--ease-out-quad) both
                }

                .glass {
                  background: linear-gradient(to bottom right, rgba(8, 8, 20, .6), rgba(15, 15, 40, .55));
                  backdrop-filter: blur(10px)
                }

                .ring-gradient {
                  box-shadow: 0 0 0 1px rgb(30 41 59 / .5), inset 0 0 0 1px rgb(99 102 241 / .25)
                }
              </style>
              <%- (typeof extraHead !=='undefined' ? extraHead : '' ) %>
</head>

<body class="min-h-screen bg-gradient-to-br from-[#0B1020] via-slate-950 to-[#0b1324] text-slate-100 animate-fade-in">
  <script type="text/javascript">
    (() => {
      const name = 'outbound-link-click';
      document.querySelectorAll('a').forEach(a => {
        if (a.host !== window.location.host && !a.getAttribute('data-umami-event')) {
          a.setAttribute('data-umami-event', name);
          a.setAttribute('data-umami-event-url', a.href);
        }
      });
    })();
  </script>